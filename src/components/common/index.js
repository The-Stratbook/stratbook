// Import and export all common components from a single file
import Button from './Button';
import Card from './Card';
import Loading from './Loading';
import Section from './Section';
import Typography from './Typography';

// Export individual typography components for direct import
export const { Heading, Text, Caption, Badge } = Typography;

// Export all components
export {
  Button,
  Card,
  Loading,
  Section,
  Typography
};